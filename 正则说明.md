# 自定义正则配置完整使用文档（Word文档处理专用）
## 目录
1. 自定义替换正则 使用规范
2. 一级大纲匹配正则 使用规范
3. 全局通用强制规则

# 一、自定义替换正则
## 1.1 语法标准
1. 单条规则独占一行，固定分隔符 `||`，标准格式：`匹配正则||替换文本`
2. 行首以 `#` 开头的整行内容为注释，程序自动跳过，仅用于阅读备注
3. 删除逻辑：`||` 后方无任何字符 = 匹配到的内容直接删除清空
4. 执行顺序：从上至下逐条全局替换，上方规则优先级更高
5. 空输入框：不执行自定义替换逻辑，仅使用软件内置处理规则
6. 空白行：文本框内纯空白行会被程序忽略，不生成处理规则

## 1.2 Word文档专用常用规则示例
| 序号 | 匹配正则 | 替换内容 | 功能说明 |
| ---- | ---- | ---- | ---- |
| 1 | `\n{2,}` | `\n` | 压缩2个及以上连续空行，统一为单行换行 |
| 2 | `　` | ` ` | Word全角空格/不间断空格替换为标准半角空格 |
| 3 | `\t` | （空） | 清除文档内所有制表符 |
| 4 | `^\s+` | （空） | 删除每行开头全部空白（空格、制表符） |
| 5 | `\s+$` | （空） | 删除每行末尾全部空白（空格、制表符） |
| 6 | `\v` | `\n` | Word软换行（手动换行符↓）转为标准段落换行 |
| 7 | `\f` | （空） | 删除全文分页符 |
| 8 | `\f{2,}` | `\f` | 多个连续分页符压缩为单个分页符 |
| 9 | `０` | `0` | 全角数字0转为半角数字0 |
| 10 | `１` | `1` | 全角数字1转为半角数字1 |
| 11 | `２` | `2` | 全角数字2转为半角数字2 |
| 12 | `３` | `3` | 全角数字3转为半角数字3 |
| 13 | `４` | `4` | 全角数字4转为半角数字4 |
| 14 | `５` | `5` | 全角数字5转为半角数字5 |
| 15 | `６` | `6` | 全角数字6转为半角数字6 |
| 16 | `７` | `7` | 全角数字7转为半角数字7 |
| 17 | `８` | `8` | 全角数字8转为半角数字8 |
| 18 | `９` | `9` | 全角数字9转为半角数字9 |
| 19 | `([，。！？；：])(\w)` | `$1 $2` | 中文标点后自动添加半角空格 |
| 20 | `\(\s*\)` | （空） | 删除空英文括号 () |
| 21 | `（\s*）` | （空） | 删除空中文括号 （） |
| 22 | `&nbsp;` | ` ` | 清除网页复制残留 &nbsp; 空白占位符 |
| 23 | `\s+\n` | `\n` | 清理换行前多余空白字符 |
| 24 | `、+` | `、` | 多个连续顿号压缩为单个顿号 |

## 1.3 转义字符说明
### Word特殊控制字符
- `\n`：Word段落换行标记
- `\v`：Word手动软换行（向下箭头）
- `\f`：Word分页符
- `\t`：制表符

### 正则特殊符号转义
`.` `+` `*` `?` `(` `)` `[` `]` `\` 等符号需要前置反斜杠转义
示例：小数点 `\.`、左括号 `\(`

# 二、一级大纲匹配正则（Word标题识别）
## 2.1 语法标准
1. 一行仅填写一条匹配正则，无分隔符
2. 行首 `\#` 开头为注释行，不参与文本匹配
3. 生效模式：追加式规则，不会覆盖软件原生标题识别逻辑，自定义规则与内置规则同时生效
4. 判定逻辑：文档任意一行命中任意一条正则，则该行标记为一级大纲标题
5. 空输入框：仅使用软件内置标题识别规则

## 2.2 Word标题匹配完整示例
| 序号 | 匹配正则 | 匹配目标说明 |
| ---- | ---- | ---- |
| 1 | `^\# .+` | Markdown一级标题，例：`\# 第一章` |
| 2 | `^[一二三四五六七八九十]+、.+` | 中文大写序号标题，例：`一、概述` |
| 3 | `^（[一二三四五六七八九十]+）.+` | 中文括号序号标题，例：`（一）内容` |
| 4 | `^\d+\. .+` | 阿拉伯数字序号标题，例：`1. 前言` |
| 5 | `^\(\d+\) .+` | 数字括号标题，例：`(1) 简介` |
| 6 | `^【.+】` | 方括号标题，例：`【第一章】` |
| 7 | `^[ⅠⅡⅢⅣⅤⅥⅦⅧⅨⅩ]+、.+` | 罗马数字序号标题，例：`Ⅰ、总则` |
| 8 | `^第[一二三四五六七八九十\d]+章 .+` | 第X章格式标题，例：`第1章 基础` |
| 9 | `^第[一二三四五六七八九十\d]+节 .+` | 第X节格式标题，例：`第二节 要点` |

## 2.3 正则符号释义
| 符号 | 功能说明 |
| ---- | -------- |
| `^` | 仅匹配行开头，精准识别行首标题序号 |
| `.+` | 匹配本行剩余全部文字 |
| `\d` | 匹配 0~9 任意阿拉伯数字 |
| `[]` | 匹配括号内任意单个字符 |

# 三、全局通用强制规范
1. 分行要求：每条规则必须单独占一行，禁止多条规则合并同一行
2. 语法支持：完全兼容 Python re 标准正则语法，不支持第三方扩展正则语法
3. 文档适配：针对Word导出纯文本、网页复制粘贴文本做特殊字符适配
4. 注释权限：可自由添加#注释行，不会影响程序运行逻辑
5. 匹配优先级：替换正则从上至下顺序执行；大纲正则全部并行匹配